version: 2
jobs:
  build:
    
    working_directory: ~/darkflame123

    docker:
      - image: circleci/openjdk:8-jdk-browsers

    steps:

      - checkout

      - restore_cache:
          key: NukkitPetteriM1Edition-{{ checksum "plugin.yml" }}
      
      - run: mvn dependency:go-offline
      
      - save_cache:
          paths:
            - ~/.m2
          key: NukkitPetteriM1Edition-{{ checksum "plugin.yml" }}
      
      - run: mvn clean package

      - store_artifacts:
          path: target/Nukkit-PM1E.jar
